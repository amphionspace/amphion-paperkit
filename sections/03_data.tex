\section{Data}
\label{sec:data}

All training and evaluation data are organised through
Lhotse~\cite{zelasko2021lhotse}'s recordings / supervisions / cuts
abstraction. The multi-task training mixture jointly draws from
seven task families: plain Mandarin / English / multilingual ASR,
hotword-conditioned ASR, target-speaker ASR (TS-ASR), speech
emotion recognition (SER), speech emotion captioning (SEC) and
environmental sound captioning (ESC).
Tables~\ref{tab:data_asr}--\ref{tab:data_aux} list the underlying
sources; Sections~\ref{subsec:tsasr-synth} and \ref{subsec:esc-mix}
describe the two synthesis pipelines that produce the additional
training cuts.

\subsection{Public ASR Sources}
\label{subsec:asr-sources}

Table~\ref{tab:data_asr} lists the 22 public ASR corpora that feed
the ASR-zh, ASR-en and code-switching streams. ``Cap.'' is the
per-source utterance cap applied before mixing; ``$r$'' is the
repeat factor (most sources are sampled once, see
Section~\ref{subsec:mixing}). A separate dataset registry handles
the multilingual long tail.

\begin{table}[t]
  \caption{Public ASR sources used for SFT. ``Cap.'' = utterance-level
    cap (``--'' = no cap). Multilingual sources are routed through the
    dataset registry and not enumerated here.}
  \label{tab:data_asr}
  \centering
  \small
  \setlength{\tabcolsep}{4pt}
  \begin{tabular}{p{2.6cm}p{6.0cm}lrr}
    \toprule
    Key & Source & Lang. & Cap. & $r$ \\
    \midrule
    librispeech         & LibriSpeech \cite{panayotov2015librispeech}                     & en       & --   & 1 \\
    mls                 & MLS English \cite{pratap2020mls}                                & en       & 500\,k & 1 \\
    common\_voice\_en   & CommonVoice 17 EN (cleaned) \cite{ardila2020commonvoice}        & en       & 500\,k & 1 \\
    gigaspeech          & GigaSpeech XL \cite{chen2021gigaspeech}                         & en       & 1\,M & 1 \\
    singaporean         & National Speech Corpus (Singapore EN)                           & en       & --   & 1 \\
    singapore\_english  & Hugging Face singapore\_english                                 & en       & --   & 1 \\
    aishell             & AISHELL-1 \cite{bu2017aishell}                                  & zh       & --   & 1 \\
    aishell2            & AISHELL-2 \cite{du2018aishell2}                                 & zh       & 500\,k & 1 \\
    aishell3            & AISHELL-3 \cite{shi2021aishell3}                                & zh       & --   & 1 \\
    magicdata           & MagicData Mandarin \cite{magicdata}                             & zh       & --   & 1 \\
    aidatatang          & aidatatang\_200zh                                               & zh       & --   & 1 \\
    primewords          & Primewords Chinese 100h                                         & zh       & --   & 1 \\
    thchs30             & THCHS-30 \cite{wang2015thchs30}                                 & zh       & --   & 1 \\
    common\_voice\_zh   & CommonVoice 17 ZH-CN \cite{ardila2020commonvoice}               & zh       & --   & 1 \\
    wenetspeech         & WenetSpeech \cite{zhang2022wenetspeech}                         & zh       & 1\,M & 1 \\
    wenetspeech4tts     & WenetSpeech4TTS                                                 & zh       & 500\,k & 1 \\
    emilia\_zh          & Emilia ZH (DNSMOS-filtered) \cite{he2024emilia, reddy2021dnsmos} & zh      & 500\,k & 1 \\
    kespeech            & KeSpeech \cite{tang2021kespeech}                                & zh (8 dial.) & 500\,k & 1 \\
    talcs               & TALCS Mandarin--English code-switch \cite{li2022talcs}          & zh--en   & --   & 1 \\
    fleurs              & FLEURS (zh, en)                                                 & multi    & --   & 1 \\
    \emph{multilingual} & dataset registry: 10 langs $\times$ \{fleurs, common\_voice, yodas, mls, emilia\}
                                                                                          & multi    & per-lang & 1 \\
    \bottomrule
  \end{tabular}
\end{table}

\paragraph{Language tag canonicalisation.}
Source manifests use a mixture of ISO-639-1, ISO-639-3 and locale tags
(``zh'', ``zh-cn'', ``en-us'', ``eng''). The data conversion layer
normalises every supervision-level language tag to a Title-Case
English name (e.g.\ ``zh''\,$\to$\,Chinese, ``en-us''\,$\to$\,English)
across the eighteen languages we currently support
(zh, en, ja, ko, fr, de, es, ru, pt, it, nl, tr, ar, hi, vi, th, id, ms);
unknown values are preserved verbatim.

\subsection{Hotword-Conditioned ASR Sources}
\label{subsec:hotword-sources}

Three of the public ASR datasets carry, on every utterance, a
ground-truth list of rare entities mined offline from the transcript
(Table~\ref{tab:data_hotwords}). At training time we do not re-mix
the audio; the same waveform is paired with the hotword-bearing
supervision to teach the model the prompt-conditioned biasing
behaviour. Two CommonVoice splits with the same annotation are
reserved for evaluation, after applying the per-utterance QA
acceptance flag shipped with each split.

\begin{table}[t]
  \caption{Hotword-bearing supervision splits. The audio is the same
    as the corresponding row in Table~\ref{tab:data_asr}; only the
    supervision metadata is augmented with the ground-truth hotword
    list.}
  \label{tab:data_hotwords}
  \centering
  \small
  \begin{tabular}{llcr}
    \toprule
    Split & Underlying audio & Use & $r$ \\
    \midrule
    aidatatang hotwords    & aidatatang\_200zh & train & 3 \\
    magicdata hotwords     & MagicData         & train & 2 \\
    gigaspeech hotwords    & GigaSpeech XL     & train (cap 1\,M) & 1 \\
    \midrule
    CV ZH hotwords         & CommonVoice ZH test & test (QA-filtered) & -- \\
    CV EN hotwords         & CommonVoice EN test & test (QA-filtered) & -- \\
    \bottomrule
  \end{tabular}
\end{table}

\subsection{Auxiliary Tasks (SER, SEC, ESC)}
\label{subsec:aux-sources}

Beyond ASR, the same model is jointly trained on speech emotion
recognition (single-label classification across eight categories
\{Neutral, Happy, Sad, Angry, Fear, Disgust, Surprise, Other/Complex\}),
speech emotion captioning (single-sentence emotion description) and
environmental sound captioning. Sources are listed in
Table~\ref{tab:data_aux}; a unified manifest preparation step maps
every source's raw label inventory to the eight-class taxonomy above.

\begin{table}[t]
  \caption{SER / SEC / ESC training sources. Each SER and SEC source
    is repeated five times to balance the mux weight against the
    larger ASR streams; ESC is repeated ten times.}
  \label{tab:data_aux}
  \centering
  \small
  \begin{tabular}{lllr}
    \toprule
    Task & Source & Lang. & $r$ \\
    \midrule
    SER  & BIIC Podcast            & zh    & 5 \\
    SER  & M3ED                    & zh    & 5 \\
    SER  & MELD                    & en    & 5 \\
    SER  & IEMOCAP                 & en    & 5 \\
    SER  & MSP-Podcast             & en    & 5 \\
    SER  & Emotion1200 EN / ZH (self-built) & en/zh & 5 \\
    SEC  & Emotion1200 EN / ZH (sec splits) & en/zh & 5 \\
    SEC  & Emotion1200 EN / ZH (batch~2)    & en/zh & 5 \\
    ESC  & AudioSet ESC subset ($\sim$218\,k cuts) & multi & 10 \\
    \bottomrule
  \end{tabular}
\end{table}

\subsection{Augmentation Resources}
\label{subsec:aug-resources}

Two pools of side data are used by the TS-ASR synthesis pipeline
(Section~\ref{subsec:tsasr-synth}): room impulse responses (RIRs) and
additive noise. RIR convolutions are truncated to $1.5$\,s (early
reflections dominate intelligibility) and applied via an FFT-based
overlap-add. Noise is mixed at a configured signal-to-noise ratio
(SNR) and the result is peak-normalised to avoid clipping.

\begin{table}[t]
  \caption{Room impulse response and noise pools used by the TS-ASR
    synthesiser. Within each pool, RIRs are sampled uniformly; noise
    sources are sampled with the listed weights per training example.}
  \label{tab:data_rir_noise}
  \centering
  \small
  \begin{tabular}{p{4.0cm}p{6.0cm}r}
    \toprule
    Resource & Source & Weight \\
    \midrule
    SLR26 sim RIRs       & OpenSLR-26 \cite{ko2017studyrir}                            & uniform \\
    SLR28 sim RIRs       & OpenSLR-28 \cite{ko2017studyrir}                            & uniform \\
    SLR28 real RIRs      & OpenSLR-28 (AIR / RWCP / REVERB-2014) \cite{ko2017studyrir} & uniform \\
    \midrule
    MUSAN noise          & SLR17 \cite{snyder2015musan}                                & 0.30 \\
    MUSAN music          & SLR17 \cite{snyder2015musan}                                & 0.20 \\
    AudioSet road traffic & AudioSet road-traffic subset                               & 0.50 \\
    \bottomrule
  \end{tabular}
\end{table}

\subsection{Subsampling and Mixing}
\label{subsec:mixing}

Mini-batches are drawn uniformly across all data sources via Lhotse's
mux operator, after the per-source caps and repeat factors of
Tables~\ref{tab:data_asr}--\ref{tab:data_aux} have been applied.
The two knobs implement different objectives:

\begin{itemize}
  \item The per-source utterance \emph{cap} truncates over-large
        corpora -- WenetSpeech and GigaSpeech at $1\,$M utterances;
        Emilia, WenetSpeech4TTS, MLS, AISHELL-2, CommonVoice EN,
        KeSpeech at $500\,$k -- so that no single source dominates
        the mux.
  \item The per-source \emph{repeat factor} $r$ up-samples small
        corpora to a comparable scale: hotword splits at $r{=}2$--$3$,
        every SER / SEC source at $r{=}5$, and AudioSet ESC at $r{=}10$
        (its raw $\sim$218\,k cuts become $\sim$2.2\,M after
        repetition, matching the major ASR streams).
\end{itemize}

The synthesised TS-ASR cut set (Section~\ref{subsec:tsasr-synth}) is
added with $r{=}1$ because its $\sim$3\,M positive cuts already match
the major ASR streams in size.

\subsection{TS-ASR Synthesis}
\label{subsec:tsasr-synth}

The target-speaker training data is produced by an offline
synthesiser. Given 16 single-speaker source manifests (10 Mandarin +
6 English; Table~\ref{tab:tsasr_sources}), each output sample contains
a target waveform mixed with $K$ interferers, an enrollment utterance
from the same speaker but \emph{a different recording}, and the
target's transcript:

\begin{equation}
  \mathrm{mixed}[n] = s_\text{tgt}[n] + \sum_{k=1}^{K}\,\alpha_k\,s_{\text{int},k}[n - o_k],
  \qquad
  \alpha_k = \frac{\rho_\text{tgt}}{\rho_{\text{int},k}}\cdot 10^{-\mathrm{SNR}_k/20},
  \label{eq:mixspeakers}
\end{equation}

where $\rho$ denotes RMS energy, $o_k$ is a uniformly sampled time
offset that places interferer $k$ inside the target, and the overlap
fraction $|\text{int}_k|/|\text{tgt}|\!\sim\!\mathrm{Uniform}[0.1, 1.0]$
controls how much of the target the interferer covers. The
``clean'' default configuration draws
$K\!\sim\!\mathrm{Cat}(\{1{:}0.8, 2{:}0.2\})$,
$\mathrm{SNR}\!\sim\!\mathcal{N}(5, 7^2)$\,dB clipped to $[-5, 20]$,
and $3$--$5$\,s enrollments. After mixing we apply an RIR with
probability $0.5$ (Table~\ref{tab:data_rir_noise}) and an additive
background noise segment with $\mathrm{SNR}\!\sim\!\mathrm{Uniform}(0, 25)$\,dB.

\begin{table}[t]
  \caption{Single-speaker source manifests fed to the TS-ASR
    synthesiser. The synthesiser drops every speaker with $<\!2$
    utterances and every utterance flagged as failing the per-corpus
    QA check; speakers, languages and source datasets are indexed
    independently to allow cross-source interferer sampling.}
  \label{tab:tsasr_sources}
  \centering
  \small
  \begin{tabular}{lp{6.0cm}p{3.5cm}}
    \toprule
    Lang. & Sources & Coverage \\
    \midrule
    zh (10) & AISHELL-1/2/3, MagicData, KeSpeech, aidatatang\_200zh, Primewords, THCHS-30, CommonVoice ZH, Emilia ZH & full speaker pools, capped per speaker \\
    en (6)  & LibriSpeech (clean-100/360, other-500), MLS English, CommonVoice EN, Emilia EN & same \\
    \bottomrule
  \end{tabular}
\end{table}

\paragraph{Anti-hallucination negatives.}
Five to ten percent of every training shard is replaced by
\emph{negative} samples whose ground-truth transcript is the empty
string. Two sub-types (Table~\ref{tab:tsasr_negatives}) are sampled
with equal weight; both preserve a real enrollment but remove the
target speaker from the mixed channel, training the model to remain
silent when the requested speaker is absent.

\begin{table}[t]
  \caption{Anti-hallucination negative sub-types.}
  \label{tab:tsasr_negatives}
  \centering
  \small
  \setlength{\tabcolsep}{4pt}
  \begin{tabular}{lp{4.5cm}p{6.5cm}}
    \toprule
    Sub-type & Composition & Key parameters \\
    \midrule
    Noise-only       & Real enrollment + 1--2 noise / silence segments & duration $[3,15]$\,s; gain $[-30, 0]$\,dB; silence inclusion probability $0.1$; sources weighted road-traffic $0.6$, MUSAN noise $0.3$, MUSAN music $0.1$ \\
    Distractor-only  & Real enrollment + 1--3 non-target speakers      & SNR $[-5, 20]$\,dB; overlap fraction $[0.1, 1.0]$ \\
    \bottomrule
  \end{tabular}
\end{table}

\paragraph{Output and scale.}
The default budget is $3{,}000{,}000$ training samples, with up to
$200$ samples per speaker. Section~\ref{sec:tsasr} reports
evaluation results on a held-out $10\,$k-cut test set generated by
the same pipeline with a held-out random seed to guarantee zero
overlap with training.

\subsection{ESC Foreground Mixing}
\label{subsec:esc-mix}

An on-the-fly foreground mixer pairs each AudioSet ESC ``background''
cut with an Emilia foreground utterance
(DNSMOS-filtered~\cite{he2024emilia, reddy2021dnsmos}) of
$0.5$--$10$\,s. The two are combined under one of three modes drawn
with probabilities $\{$overlap $0.8$, prepend $0.1$, append $0.1\}$;
SNR is $\mathrm{Uniform}(0, 20)$\,dB by default, and unconstrained
when the background is itself a silence cut (the foreground simply
plays over the silence). Validation and the offline ESC test set
($\sim$$2{,}166$ cuts) use a fixed mix seed so that loss curves stay
comparable across checkpoints.

\subsection{Hotword Pool Construction at Training Time}
\label{subsec:hotword-pool}

Training-time hotword lists are assembled per sample in three stages:

\begin{enumerate}
  \item \textbf{Real terms.} Each ground-truth hotword is
        independently dropped with probability $0.05$ to simulate
        imperfect retrieval recall.
  \item \textbf{Hard negatives.} An online retriever fetches up to
        $K\!=\!10$ confusable terms per sample using the weighted
        score $0.6\cdot\text{char-overlap} +
        0.4\cdot\text{bigram-Jaccard}$ over a per-corpus hotword
        index, augmented for Mandarin by a syllable-level
        nearest-neighbour search.
  \item \textbf{Random distractors.} The remaining
        $n_\text{distractor}\!\in\![1, 20]$ slots are filled from the
        global hotword pool, with the hard-to-random ratio set so
        that hard negatives saturate first.
\end{enumerate}

The whole hotword prompt line is then kept with probability $0.8$
and dropped otherwise, training the same model to behave gracefully
when no hotword side-channel is available at inference time.

\subsection{Evaluation Datasets and Metrics}
\label{subsec:eval-data}

All evaluations are dispatched through a unified dataset registry,
shared between the offline (icefall-style) decoder and the online
batched inference server. Table~\ref{tab:eval} lists the test sets
reported in this paper. Per-language WER and CER, silence recall on
negative TS-ASR samples, classification accuracy and macro-F1 for
SER, and tag-set micro / macro F1 plus ROUGE-L for ESC are computed
by a single evaluation harness, so that all numbers in the paper come
from the same metric implementation.

\begin{table}[t]
  \caption{Evaluation sets used in this report. The hotword sweep
    pads each test utterance to $k$ in-prompt hotwords by adding
    random distractors drawn from the same corpus.}
  \label{tab:eval}
  \centering
  \small
  \begin{tabular}{lp{8.5cm}l}
    \toprule
    Task & Test sets & Metric \\
    \midrule
    ASR-zh        & AISHELL-1/2/3, MagicData, KeSpeech, THCHS-30,
                    CommonVoice ZH, WenetSpeech (net / meeting),
                    FLEURS ZH                                                  & CER \\
    ASR-en        & LibriSpeech (test-clean / -other), GigaSpeech, MLS,
                    CommonVoice EN, FLEURS EN                                  & WER \\
    Code-switch   & TALCS test \cite{li2022talcs}                              & MER \\
    Hotword ASR   & CommonVoice ZH / EN hotword splits (sweep
                    $k\!\in\!\{0, 5, 10, 15, 20\}$)                            & CER / WER \\
    TS-ASR        & internal hot+TS test ($n\!=\!6{,}227$, with $5$\,\%
                    silent negatives) + LibriMix 2 / 3 \cite{cosentino2020librimix}
                                                                              & MER + silence \\
    \bottomrule
  \end{tabular}
\end{table}
