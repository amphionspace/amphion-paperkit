\begin{abstract}
We present \textbf{LLM-ASR}, a large language model based automatic speech recognition system that
unifies a strong audio encoder with a pre-trained large language model decoder.
% TODO: 1-2 sentences on motivation / gap.
% TODO: 1-2 sentences on approach (encoder choice, adapter, training stages).
% TODO: 1-2 sentences on data scale and languages covered.
% TODO: 1-2 sentences on key results (WER on benchmark X, relative improvement Y\% over baseline Z).
% TODO: 1 sentence on release (weights / data / code).
We release model weights and inference code at \url{https://github.com/your-org/llm-asr}.
\end{abstract}
