---
description: 本报告所有评测数字以 refs/docs/data.xlsx 和 refs/docs/target_speaker_bench.md 为真相源；所有数字都来自同一个 AmphionASR 模型；过往历史数字一律作废。
alwaysApply: true
---

# 评测数据真相源 (Single Source of Truth)

> 仅作用于 `egs/amphion-asr-2026/`。与公共层 [AGENTS.md 规则 5](../../../AGENTS.md) 与 [paper-writing.mdc §5](../../../.cursor/rules/paper-writing.mdc) 协同；当本规则与历史 `sections/*.tex`、`refs/docs/*.md`、过往会话中的数字冲突时，以本规则为准。

## 1. 唯一真相源

本报告 AmphionASR 自身的评测数字按章节区分两个真相源：

| 真相源文件 | 覆盖章节 | 备注 |
| --- | --- | --- |
| [`refs/docs/data.xlsx`](../../refs/docs/data.xlsx) | §6 / §7 / §9 | 5 个 sheet：ASR / ASR-hotwords / TS-ASR / Noise-Robust / voice-in-the-wild-bench |
| [`refs/docs/target_speaker_bench.md`](../../refs/docs/target_speaker_bench.md) | §8 | xlsx 中 TS-ASR sheet 当前为空；§8 主表的 AmphionASR 行 + 3 个开源 baseline 全部从该 md 文件第 3 / 第 5 节取 |

`data.xlsx` 各 sheet 详细结构：

| Sheet | 形状 | 结构要点 | 覆盖章节 |
| --- | --- | --- | --- |
| ASR | 11×8（含 header 行 1） | 行 = 10 个 baseline 模型 + 1 行 AmphionASR；列 = `Model/Datasets` + `LibriSpeech (clean \| other)` + `GigaSpeech` + `WenetSpeech (net \| meeting)` + `AIShell1` + `AIShell2` + `KeSpeech` + `CommonVoice (en \| zh)`；多数 cell 是单个数字，含 `\|` 或全宽 `｜` 的 cell 是 "clean \| other" 这类两值组合 | §6 Experiments 两主表（[tab:asr_en / tab:asr_zh](../../sections/06_experiments.tex)），实际表内容由 `tables/asr_en.tex` / `tables/asr_zh.tex` 承载 |
| ASR-hotwords | 41×13，含 4 个 block × 各自的 header row | block 划分：行 1 `v4(random)` / 行 12 `v4(retrieve)` / 行 22 `fun-asr-nano（random）` / 行 33 `fun-asr-nano（retrieve）`；每 block 内 `commonvoice_zh_hotwords` + `commonvoice_en_hotwords` × K∈{0,5,10,15,20}（retrieve block 无 K=0）；retrieve block 列宽 13，random block 列宽 11；指标列 `KER %` / `SACC %` / `B-WER %` / `U-WER %` / `PRR %` / `PF1 %` / `WER %`，retrieve block 多 `Recall@K %` / `PrRR %` | §7 Hotword Conditioning 三张表（[tab:hotwords_oracle / tab:hotwords_retrieve / tab:hotwords_baseline](../../sections/07_hotwords.tex)） |
| TS-ASR | 当前为空（曾用结构 3×3 已废） | 该 sheet 本轮迁移到 `refs/docs/target_speaker_bench.md`，xlsx 暂留空 placeholder；不要再从该 sheet 抽 §8 数字 | §8（数据从 `target_speaker_bench.md` 取） |
| Noise-Robust | 68×20（行 14 之后部分空）；行 2 为 header | 历史 §9 主表数据源，本轮 §9 已整章换成 vitw-bench，该 sheet 在 paper 内不再被引用；保留作历史归档 | 无（§9 不再使用） |
| voice-in-the-wild-bench | 17×11（含 header 行 1） | 行 = 16 个 vitw 子集（real 8 + sim 8，每组含 7 atomic effects + 1 mixed）；列 = `Dataset` / `n` / `vitw-sft`(=AmphionASR) / `mega-asr` / `qwen3` / `gemini3-flash` / `seed-asr` / `gpt-4o-trans` / `whisper-l-v3` / `qwen2.5-omni` / `kimi-audio`；baseline 8 列数据 verbatim 引用自 [Xie et al. 2026 Mega-ASR Table 4](../../refs/notes/papers/xie2026-mega-asr.pdf)，AmphionASR 列由我们自跑 | §9 Noise Robustness 主表（[tab:vitw_bench](../../sections/09_noise_robustness.tex)） |

外部 baseline（Whisper-large-v3、Kimi-Audio、Step-Audio 2、MiniCPM-o、MOSS-Audio、MiMo-Audio、Qwen3-ASR-1.7B、FireRed-ASR2-LLM、GLM-ASR-nano 在 §6 baseline 列；Qwen3-ASR-{0.6B,1.7B}、Qwen3-Omni 在 §8 baseline 行；Mega-ASR / Gemini-3-Flash / Seed-ASR / GPT-4o-transcribe / Qwen2.5-Omni 在 §9 vitw 表）的数字仍按 [paper-writing.mdc §5 lookup order](../../../.cursor/rules/paper-writing.mdc) 走（refs/notes/papers → WebFetch / WebSearch），与本规则互补、不冲突。`fun-asr-nano` 的 hotword 数字虽落在 `ASR-hotwords` sheet 中，但来自外部商用模型评测、属于第三方 baseline。

## 2. 单一模型口径

`data.xlsx` 和 `target_speaker_bench.md` 中所有 AmphionASR 相关列对应同一个开源 AmphionASR 模型。具体绑定：

| 真相源列 / 行 | paper 中的口径 | 性质 |
| --- | --- | --- |
| `ASR` sheet 第 2 行 `AmphionASR` | `AmphionASR`（[§6 tab:asr_en/zh](../../sections/06_experiments.tex)） | 开源版本，唯一发布产物 |
| `ASR-hotwords` sheet 中 `v4(random)` / `v4(retrieve)` 两个 block | `AmphionASR`（[§7 tab:hotwords_oracle / tab:hotwords_retrieve](../../sections/07_hotwords.tex)） | 同上 |
| `ASR-hotwords` sheet 中 `fun-asr-nano（random）` / `fun-asr-nano（retrieve）` 两个 block | `fun-asr-nano`（[§7 tab:hotwords_baseline](../../sections/07_hotwords.tex)） | 第三方商用 baseline，非本模型 |
| `target_speaker_bench.md` §3 表中 `Amphion-4B-vitw-v1-SFT` 行 | `AmphionASR`（[§8 tab:tsasr](../../sections/08_ts_asr.tex)） | 同上（即 ckpt `Amphion-4B-vitw-v1-SFT`） |
| `target_speaker_bench.md` §5 表中 `Amphion-4B-vitw-v1-SFT` 行 | `AmphionASR`（同 §8 表 LibriMix 列） | 同上 |
| `voice-in-the-wild-bench` sheet 中 `vitw-sft` 列 | `AmphionASR`（[§9 tab:vitw_bench](../../sections/09_noise_robustness.tex)） | 同上 |

禁止在 paper 中再出现任何指代历史迭代的版本号自指字样：

- `v3 checkpoint` / `v3 release` / `v3 SFT recipe` / `AmphionASR v3`
- `v3-NR variant` / `v3-NR` / `v4`
- `Amphion-4B-vitw-v1-SFT` 这种 ckpt 命名只在 `.cursor/rules` 和 `refs/docs/` 内部出现；paper 一律用 `AmphionASR`
- 任何 `\label{subsec:v3nr}` 之类的 anchor

例外保留：`Whisper-large-v3` / `Qwen3-ASR-1.7B` / `Whisper-large-v2` 等是第三方产品名。

## 3. 过往历史数据作废

下列来源的旧数字一律作废，不得作为 paper 内事实依据：

1. 早期 `sections/*.tex` 历史 commit 中已被覆盖的数字：
   - LibriSpeech test-clean: `7.17` / `1.82` 均已被作废，现采用 `1.55`（ASR sheet）
   - AISHELL-1: `1.85` 已作废，现采用 `1.51`（ASR sheet, `AIShell1` 列）
   - TALCS MER: `14.99` / `7.48` 均已作废；TALCS 本轮已从 §6 主表移除（未在 xlsx 覆盖范围内）
   - LibriMix: `66.14` / `86.71` 已作废，现采用 `66.33` / `86.00`（target_speaker_bench.md §5）
   - Internal hot+TS MER: `14.29` / `13.06` 已作废；新口径用 ts_hw_test positive WER `12.56`
   - Hotword CV17-zh oracle: `6.57` / `2.83` / `6.01` / `2.67` 均已作废，现采用 `5.93` / `2.82`（v4 random）
   - Hotword CV17-en oracle: `9.65` / `4.89` / `7.68` / `4.71` 均已作废，现采用 `7.79` / `5.01`（v4 random）
   - Noise-Robust 表（CommonVoice + AudioSet/MUSAN/RIR 那套）整套数字 + 论述 已被 vitw-bench 替代；NR-SFT MUSAN-speech 41.19→18.74 / 21.45→9.45 这种 ablation 在 paper 内不再出现
   - FLEURS-en `30.47` 已作废；FLEURS 整体本轮不在 §6 主表内
2. `refs/docs/plan.md`、`refs/docs/model_arch.md`、`refs/docs/train_eval_data.md`、`refs/docs/task_prompts.md` 等内部工作文档中的任意数字 —— 这些是工作记录，不是 paper 真相源（见 [AGENTS.md 规则 5](../../../AGENTS.md)）。若与 `data.xlsx` / `target_speaker_bench.md` 不一致，以真相源为准
3. 过往会话（agent 历史 transcript）中曾经被引用过的本模型自家数字
4. README / blog / 任何 model card 草稿中尚未对齐到真相源的 AmphionASR 数字

若在内部交流中需要引用历史数字（例如复盘"为什么过去 LibriMix 67% / 66.14%、现在 66.33%"），明确加上"历史值，已被真相源取代"的注明，不要让历史值流回 paper 出口。

## 4. 冲突处理

当 `refs/docs/`、过往 `sections/*.tex`、内部记忆三者之一与真相源出现数字冲突：

1. 优先采纳真相源（按 §1 分章节查 xlsx 或 target_speaker_bench.md）的值
2. 用 `tools/fact-check-regex.sh egs/amphion-asr-2026` 扫一遍 paper，确认无规则 4（无实现细节）违规
3. 若旧值仍残留在某行（例如 abstract / conclusion 头条），用 grep 定位后统一替换
4. 在 reply 的 fact-check block 列出"旧值 → 新值"映射、来源 sheet / md 文件与 dataset+列名锚点（见 §5）

## 5. 引用要求

每次涉及表格 / 数字修改的 reply，fact-check block 必须显式列出：

- 来源 sheet 名（`ASR` / `ASR-hotwords` / `voice-in-the-wild-bench`）或 md 文件 + 节号（`target_speaker_bench.md §3` / `§5`）
- 来源锚点：优先用 (dataset 名 / 行名 + 列名 + 数值) 三元组（行号会随 sheet 扩展漂移；用 dataset+列名 作为稳定锚点）
- 与 [paper-writing.mdc §6](../../../.cursor/rules/paper-writing.mdc) 的 fact-check 要求一致；本规则只是细化 AmphionASR 自家数字的引用粒度

例（用 dataset+列名 锚点；行号仅作辅助）：

> Fact check:
> - AIShell-1 1.51 % CER → `refs/docs/data.xlsx` `ASR` sheet, `Model=AmphionASR, AIShell1=1.51`，已确认
> - LibriSpeech test-clean 1.55 % WER → `ASR` sheet, `Model=AmphionASR, LibriSpeech (clean \| other)` 的第一个值，已确认
> - TS-ASR ts_hw_test positive 12.56 % WER → `refs/docs/target_speaker_bench.md` §3 主表 `Amphion-4B-vitw-v1-SFT` 行 `WER=12.56`，已确认
> - TS-ASR libri2mix 66.33 % WER → `target_speaker_bench.md` §5 LibriMix 表 `Amphion-4B-vitw-v1-SFT` 行 `libri2mix WER=66.33`，已确认
> - vitw Real Obstructed 1.29 % WER → `voice-in-the-wild-bench` sheet, `Dataset=vitw_real_obstructed, vitw-sft=1.29`，已确认
> - vitw Real Obstructed Mega-ASR baseline 1.80 % WER → 同 sheet, `Dataset=vitw_real_obstructed, mega-asr=1.80`，原始来源 `refs/notes/papers/xie2026-mega-asr.pdf` Table 4，已确认
> - Hotword CV17-zh K=10 retrieve WER → `ASR-hotwords` sheet, `v4(retrieve)` block, `commonvoice_zh_hotwords, Top-K=10, WER %=3.60`（行 14），已确认

## 6. 更新流程

`data.xlsx` 或 `target_speaker_bench.md` 一旦更新（用户交付新版本），按以下顺序执行：

1. 先读全部 sheet / 全部章节，不要只读某一部分
2. 与当前 `sections/*.tex` 的所有表格逐项对比，输出"旧值 → 新值"映射表给用户确认
3. 不允许保留"旧数字 + 新数字混合"的中间提交；要么完整对齐到新真相源，要么不动
4. 修完所有 `sections/*.tex` 后跑 `latexmk -pdf main.tex`，确认 0 warning + PDF 中旧值出现次数为 0
5. 同步更新 abstract / introduction / conclusion 中的 headline 数字（这是 reviewer 最先读的位置，最容易遗漏）
6. fact-check block 列出更新的"旧值 → 新值"映射

## 7. 与其它规则的关系

- 与 [AGENTS.md 规则 3](../../../AGENTS.md)（fact check）联动：本规则把 fact check 的"自家数字"行的来源精确到真相源 sheet / md + dataset 名 + 列名（见 §5）
- 与 [AGENTS.md 规则 5](../../../AGENTS.md)（refs/docs 是原料库，不清楚要问）联动：refs/docs/*.md（除 `target_speaker_bench.md`）中的数字不是真相源；其余 md 都是工作记录
- 与 [AGENTS.md 规则 9](../../../AGENTS.md)（不自作聪明）联动：真相源中的反常数字（例如 fun-asr-nano retrieve Recall@K ≈ 0%）按规则 9 一笔带过，不要补 mechanism
- 与 [writing-style.mdc §10](writing-style.mdc) 联动：§10.1 列出的 14 项已 cover 外部事实仍按"refs/notes/papers + WebFetch"走；本规则仅约束 AmphionASR 自家数字
