% Data.
%
% Per .cursor/rules/asr-domain.mdc §3, every data source must report:
%   name, hours, utterance / segment count, language(s) (ISO code), license
%   (SPDX), domain category, collection year, citation.
% Cleaning pipeline must be described transparently (VAD, language ID,
% confidence filter, dedup, PII / profanity scrubbing).
% Per-source table must sum to a clear "Total" row.

\section{Data}
\label{sec:data}

% TODO: 1--2 paragraph overview of the data strategy (multi-source, languages,
% scale, why this composition).

\subsection{Sources}
\label{subsec:data_sources}

% TODO: per-source table here. Replace the placeholder with a real one.

\subsection{Cleaning Pipeline}
\label{subsec:data_cleaning}

% TODO: VAD model + threshold; language ID model + threshold; confidence
% filter (if any); dedup strategy; PII / profanity scrubbing.
