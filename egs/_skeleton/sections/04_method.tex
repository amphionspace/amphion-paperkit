% Method / model.
%
% Per .cursor/rules/asr-domain.mdc §4, for every component report:
%   - module name (Encoder / Adapter / LLM / ...)
%   - backbone identity and version (e.g. Whisper-large-v3 encoder)
%   - parameter count (one decimal in M or B)
%   - hidden dim, layers, attention heads
%   - frame rate (Hz) at module boundaries
%   - frozen / trainable / LoRA-adapted in each training stage
% Prompt templates: paste literal templates with the `plain` listing style
% defined in template/amphion.cls; do not paraphrase.

\section{Method}
\label{sec:method}

% TODO: 1-paragraph overview of the architecture; refer to a TikZ figure
% built from figures/architecture-skeleton.tex if provided.

\subsection{Architecture}
\label{subsec:method_arch}

% TODO: per-module description.

\subsection{Inputs and Outputs}
\label{subsec:method_io}

% TODO: prompt templates (verbatim, not paraphrased), tokenisation, output
% format.
