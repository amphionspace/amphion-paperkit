\section{Introduction}
\label{sec:intro}

\begin{figure}[t]
  \centering
  \includegraphics[width=0.92\linewidth]{figures/capabilities-v4.png}
  \caption{Three recognition settings illustrated for
    AmphionASR.
    \emph{Top-left:} hotword conditioning with retrieval-based
    contextual biasing.
    \emph{Top-right:} target-speaker ASR with a $3$--$5$\,s
    enrollment utterance.
    \emph{Bottom:} degradation-robust ASR via SFT on
    Voice-in-the-Wild-2M~\cite{xie2026mega}.}
  \label{fig:teaser}
\end{figure}

Speech recognition has progressed from hybrid HMM--DNN systems
\cite{hinton2012deep} to end-to-end and weakly supervised models such
as Whisper~\cite{radford2023whisper}.
Recent SpeechLLMs pair audio encoders with autoregressive language
backbones~\cite{chu2023qwenaudio,tang2024salmonn,stepaudio22025,kimiaudio2025,qwen2025qwen3asr}.

General transcription is only one part of practical ASR.
A recogniser may need a domain vocabulary, a reference utterance that
identifies the target speaker, or explicit handling of degraded and
whispered speech.
These settings differ in their side information and acoustic
conditions, but they share the same desired output: the transcript of
the requested speech.

We present \textbf{AmphionASR}, built on Qwen3-ASR-1.7B, as a shared
model for these four recognition settings.
Hotword lists and target-speaker enrollment audio enter as task
conditions, while degradation and whispered-speech examples adapt the
model to atypical acoustic inputs.
This formulation separates the recognition backbone from the
information that specifies what and under which conditions to
transcribe.

Our contributions are:

\begin{itemize}
  \item \textbf{A shared interface for conditioned ASR.}
        We formulate hotword-conditioned, target-speaker,
        degradation-robust, and whispered-speech ASR within one model,
        using task instructions and optional reference inputs.
  \item \textbf{Retrieval-based hotword conditioning.}
        A fine-grained dual encoder retrieves a top-$K$ subset from a
        larger vocabulary before decoding, attaining Recall@$50$ above
        $94$\,\% on CommonVoice and reducing entity error on
        GigaSpeechBench~\cite{tu2026gigaspeechbench}.
  \item \textbf{A multi-condition evaluation.}
        We evaluate the shared model on standard ASR, contextual ASR,
        target-speaker ASR, acoustic degradations, and whispered
        speech, reporting both improvements and remaining limitations.
\end{itemize}
