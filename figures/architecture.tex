\documentclass[tikz, border=8pt]{standalone}
\usepackage{tikz}
\usepackage{amsmath}
\usepackage{xcolor}
\usepackage{pifont}
\usetikzlibrary{positioning, arrows.meta, shapes.geometric, fit, backgrounds, calc, decorations.pathmorphing, patterns}

% ----------------------------------------------------------------
% Colour palette -- see AGENTS.md rule 1. Do not introduce new
% colours outside this list; new figures must reuse the same names.
% ----------------------------------------------------------------
\definecolor{cAudio}{HTML}{F2F4F7}
\definecolor{cEnc}{HTML}{D9E5F4}
\definecolor{cEncB}{HTML}{4A7BB7}
\definecolor{cAdp}{HTML}{D9F0DA}
\definecolor{cAdpB}{HTML}{4F9D58}
\definecolor{cLLM}{HTML}{FBE3CC}
\definecolor{cLLMB}{HTML}{D08A3F}
\definecolor{cPrompt}{HTML}{F7F2E8}
\definecolor{cPromptB}{HTML}{B89464}
\definecolor{cTok}{HTML}{EEE8F4}
\definecolor{cTokB}{HTML}{6A4DA8}
\definecolor{cBound}{HTML}{F2DEDE}
\definecolor{cBoundB}{HTML}{B8585F}

% Simple LaTeX-native icons (no FontAwesome / emoji dependency)
\newcommand{\frozenIcon}{\textcolor{cEncB}{\ding{100}}}
\newcommand{\fireIcon}{\textcolor{cLLMB!90!red}{\ding{72}}}

\tikzset{
  % --- module shapes (all rounded, ~same height for visual rhythm) ---
  module/.style    ={draw, rounded corners=3pt, align=center,
                     font=\sffamily\small, line width=0.7pt,
                     minimum width=3.4cm, minimum height=0.95cm},
  audio/.style     ={module, fill=cAudio, draw=black!35},
  encoder/.style   ={module, fill=cEnc, draw=cEncB},
  adapter/.style   ={module, fill=cAdp, draw=cAdpB},
  promptbox/.style ={module, fill=cPrompt, draw=cPromptB},
  tokenizer/.style ={module, fill=cTok, draw=cTokB},
  boundbox/.style  ={module, fill=cBound, draw=cBoundB},
  llmbox/.style    ={module, fill=cLLM, draw=cLLMB, line width=0.85pt,
                     minimum width=10.5cm, minimum height=1.25cm,
                     font=\sffamily},
  outbox/.style    ={module, fill=white, draw=black!50,
                     minimum width=2.8cm, minimum height=0.75cm,
                     font=\sffamily\small},
  % --- token cells ---
  tokcell/.style   ={draw, line width=0.4pt, minimum width=0.38cm,
                     minimum height=0.38cm, inner sep=0pt},
  cellText/.style  ={tokcell, fill=cTok!55, draw=cTokB!75},
  cellBound/.style ={tokcell, fill=cBound, draw=cBoundB},
  cellAudio/.style ={tokcell, fill=cAdp!90, draw=cAdpB},
  % --- arrows ---
  flow/.style      ={-Stealth, line width=0.7pt, draw=black!70},
  thinflow/.style  ={-Stealth, line width=0.55pt, draw=black!50},
  % --- text ---
  ratelabel/.style ={font=\sffamily\scriptsize, text=black!55,
                     fill=white, inner sep=1pt},
  legendtxt/.style ={font=\sffamily\scriptsize, text=black!75}
}

\begin{document}
\begin{tikzpicture}[node distance=0.55cm and 0.8cm]

  % ====================== LLM (top horizontal) ======================
  \node[llmbox] (llm) {%
    \fireIcon~~\textbf{Qwen3-4B-Instruct-2507}\\[1pt]
    \scriptsize Causal LM \,$\cdot$\, $\sim 4.41$\,B params
  };

  % output above LLM
  \node[outbox, above=0.6cm of llm] (text) {\textbf{Transcript}};
  \draw[flow] (llm.north) -- (text.south);

  % ====================== Merged input sequence (below LLM) ======================
  \coordinate (seqL) at ($(llm.south west) + (0.4cm,-1.25cm)$);
  \coordinate (seqR) at ($(llm.south east) + (-0.4cm,-1.25cm)$);
  \node[draw=black!50, dashed, rounded corners=2.5pt, line width=0.5pt,
        fit={(seqL) (seqR)}, inner sep=5pt, minimum height=0.75cm] (seqbox) {};
  \node[legendtxt, anchor=south west, text=black!55]
        at ([xshift=2pt,yshift=1pt]seqbox.north west)
        {\itshape merged \texttt{inputs\_embeds}};

  % cells inside the sequence: 3 text + 1 bound + 8 audio + 1 bound + 5 text = 18
  \foreach \i/\style in {%
      0/cellText, 1/cellText, 2/cellText,
      3/cellBound,
      4/cellAudio, 5/cellAudio, 6/cellAudio, 7/cellAudio,
      8/cellAudio, 9/cellAudio, 10/cellAudio, 11/cellAudio,
      12/cellBound,
      13/cellText, 14/cellText, 15/cellText, 16/cellText, 17/cellText%
  }{
    \pgfmathsetmacro{\xpos}{-3.65 + \i*0.43}
    \node[\style] at ([xshift=\xpos cm,yshift=-1pt]seqbox.center) (cell\i) {};
  }
  \draw[flow] (seqbox.north) -- (llm.south);

  % ====================== Audio column (left, bottom to top) ======================
  \node[adapter] at ($(seqbox.south) + (-3.4cm,-1.5cm)$) (proj) {%
    \fireIcon~~\textbf{MM Projector}\\
    \scriptsize Linear--SwooshR--Linear
  };
  \node[encoder, below=of proj] (aut) {%
    \frozenIcon~~\textbf{Qwen3-AuT Encoder}\\
    \scriptsize Whisper-style \,$\cdot$\, $\sim 314$\,M
  };
  \node[encoder, below=of aut] (front) {%
    \textbf{Log-mel + Conv Sub-sampler}\\
    \scriptsize $128$-bin, $100$\,Hz $\to \sim 13$\,Hz
  };
  \node[audio, below=of front, minimum height=0.7cm] (wave) {\textbf{Audio waveform}\,\,\scriptsize $16$\,kHz};
  % wavy pictogram above waveform
  \begin{scope}[line width=0.6pt, draw=cEncB!85]
    \draw[decoration={snake, amplitude=2pt, segment length=3pt}, decorate]
      ([xshift=-1.1cm,yshift=10pt]wave.center) -- ([xshift=1.1cm,yshift=10pt]wave.center);
  \end{scope}

  \draw[flow] (wave)  -- (front);
  \draw[flow] (front) -- (aut);
  \draw[flow] (aut)   -- (proj);

  % projector -> audio cells (with one combined merged arrow)
  \draw[flow] (proj.north) to[out=90,in=-90] ([yshift=-1pt]cell7.south);

  % ====================== Text column (right, bottom to top) ======================
  \node[boundbox] at ($(seqbox.south) + (3.4cm,-1.5cm)$) (bound) {%
    \fireIcon~~\textbf{Boundary Prompt}\\
    \scriptsize $4 \times 2560$ learnable
  };
  \node[tokenizer, below=of bound] (tok) {%
    \textbf{Qwen3 Tokenizer + Embedding}\\
    \scriptsize vocab $151{,}936$
  };
  \node[promptbox, below=of tok] (prompt) {%
    \textbf{Text prompt}\\
    \scriptsize chat template + speech tokens
  };

  \draw[flow] (prompt) -- (tok);
  \draw[flow] (tok)    -- (bound);

  % boundary -> two boundary cells; tokenizer -> text cells (left + right)
  \draw[flow] (bound.north) to[out=90,in=-90] ([yshift=-1pt]cell12.south);
  \draw[flow] (bound.north) to[out=140,in=-90,looseness=1.05] ([yshift=-1pt]cell3.south);
  \draw[thinflow] (tok.north) to[out=120,in=-90,looseness=1.0]
        ([yshift=-1pt]cell1.south);
  \draw[thinflow] (tok.north) to[out=70,in=-90,looseness=1.0]
        ([yshift=-1pt]cell15.south);

  % ====================== Compact legend (bottom-centre) ======================
  \node[draw=black!35, rounded corners=2.5pt, inner sep=5pt,
        below=0.7cm of wave, anchor=north west, line width=0.4pt, fill=white]
        (legend) {%
    \begin{minipage}{12.5cm}\sffamily\scriptsize
    \begin{tabular}{@{}l@{\hspace{12pt}}l@{\hspace{12pt}}l@{\hspace{12pt}}l@{\hspace{12pt}}l@{}}
      \frozenIcon~~frozen (Stage 1) &
      \fireIcon~~trainable &
      \tikz\node[cellText, minimum width=0.32cm, minimum height=0.32cm]{};~text embedding &
      \tikz\node[cellBound, minimum width=0.32cm, minimum height=0.32cm]{};~boundary (learnable) &
      \tikz\node[cellAudio, minimum width=0.32cm, minimum height=0.32cm]{};~audio embedding\\
    \end{tabular}
    \end{minipage}
  };

\end{tikzpicture}
\end{document}
