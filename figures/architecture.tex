\documentclass[tikz, border=8pt]{standalone}
\usepackage{tikz}
\usepackage{amsmath}
\usepackage{xcolor}
\usepackage{pifont}
\usetikzlibrary{positioning, arrows.meta, shapes.geometric, fit, backgrounds, calc, decorations.pathmorphing, patterns}

% ----------------------------------------------------------------
% Colour palette -- see AGENTS.md rule 1. Do not introduce new
% colours outside this list; new figures must reuse the same names.
% ----------------------------------------------------------------
\definecolor{cAudio}{HTML}{F2F4F7}
\definecolor{cEnc}{HTML}{D9E5F4}
\definecolor{cEncB}{HTML}{4A7BB7}
\definecolor{cAdp}{HTML}{D9F0DA}
\definecolor{cAdpB}{HTML}{4F9D58}
\definecolor{cLLM}{HTML}{FBE3CC}
\definecolor{cLLMB}{HTML}{D08A3F}
\definecolor{cPrompt}{HTML}{F7F2E8}
\definecolor{cPromptB}{HTML}{B89464}
\definecolor{cTok}{HTML}{EEE8F4}
\definecolor{cTokB}{HTML}{6A4DA8}
\definecolor{cBound}{HTML}{F2DEDE}
\definecolor{cBoundB}{HTML}{B8585F}

% Simple LaTeX-native icons (no FontAwesome / emoji dependency)
\newcommand{\frozenIcon}{\textcolor{cEncB}{\ding{100}}}
\newcommand{\fireIcon}{\textcolor{cLLMB!90!red}{\ding{72}}}

\tikzset{
  % --- module shapes ---
  module/.style    ={draw, rounded corners=3pt, align=center,
                     font=\sffamily\small, line width=0.7pt,
                     minimum width=3.5cm, minimum height=0.95cm},
  audio/.style     ={module, fill=cAudio, draw=black!35},
  encoder/.style   ={module, fill=cEnc, draw=cEncB},
  adapter/.style   ={module, fill=cAdp, draw=cAdpB},
  promptbox/.style ={module, fill=cPrompt, draw=cPromptB},
  tokenizer/.style ={module, fill=cTok, draw=cTokB},
  boundbox/.style  ={module, fill=cBound, draw=cBoundB},
  llmbox/.style    ={module, fill=cLLM, draw=cLLMB, line width=0.85pt,
                     minimum width=11.4cm, minimum height=1.25cm,
                     font=\sffamily},
  outbox/.style    ={module, fill=white, draw=black!50,
                     minimum width=2.8cm, minimum height=0.75cm,
                     font=\sffamily\small},
  % --- token cells ---
  tokcell/.style   ={draw, line width=0.4pt, minimum width=0.42cm,
                     minimum height=0.42cm, inner sep=0pt},
  cellText/.style  ={tokcell, fill=cTok!55, draw=cTokB!75},
  cellBound/.style ={tokcell, fill=cBound, draw=cBoundB},
  cellAudio/.style ={tokcell, fill=cAdp!90, draw=cAdpB},
  % --- arrows ---
  flow/.style      ={-Stealth, line width=0.7pt, draw=black!70},
  % --- text ---
  ratelabel/.style ={font=\sffamily\scriptsize, text=black!55,
                     fill=white, inner sep=1pt},
  collabel/.style  ={font=\sffamily\scriptsize\itshape, text=black!55},
  legendtxt/.style ={font=\sffamily\scriptsize, text=black!75}
}

\begin{document}
\begin{tikzpicture}

  % --------------------------------------------------------------
  % three-column grid: column centres on a single x line each, so
  % every arrow from a source module to the sequence is vertical.
  % --------------------------------------------------------------
  \def\xL{-3.9cm}   % left  column = audio path
  \def\xM{0cm}      % middle column = boundary prompt
  \def\xR{3.9cm}    % right column = text path

  % ====================== LLM (top horizontal) ======================
  \node[llmbox] at (0, 6.7) (llm) {%
    \fireIcon~~\textbf{Qwen3-4B-Instruct-2507}\\[1pt]
    \scriptsize Causal LM \,$\cdot$\, $\sim 4.41$\,B params
  };

  \node[outbox, above=0.55cm of llm] (text) {\textbf{Transcript}};
  \draw[flow] (llm.north) -- (text.south);

  % ====================== Merged input sequence (below LLM) ======
  \node[draw=black!50, dashed, rounded corners=2.5pt, line width=0.5pt,
        minimum width=11.4cm, minimum height=0.85cm,
        below=0.7cm of llm] (seqbox) {};
  \node[legendtxt, anchor=south west, text=black!55]
        at ([xshift=2pt,yshift=1pt]seqbox.north west)
        {\itshape merged \texttt{inputs\_embeds}};

  % cells inside the sequence: 4 text + 1 bound + 8 audio + 1 bound + 5 text = 19
  \foreach \i/\style in {%
      0/cellText, 1/cellText, 2/cellText, 3/cellText,
      4/cellBound,
      5/cellAudio, 6/cellAudio, 7/cellAudio, 8/cellAudio,
      9/cellAudio, 10/cellAudio, 11/cellAudio, 12/cellAudio,
      13/cellBound,
      14/cellText, 15/cellText, 16/cellText, 17/cellText, 18/cellText%
  }{
    \pgfmathsetmacro{\xpos}{-3.87 + \i*0.43}
    \node[\style] at ([xshift=\xpos cm,yshift=0]seqbox.center) (cell\i) {};
  }
  \draw[flow] (seqbox.north) -- (llm.south);

  % ====================== Three sources, top row (entries) =======
  \node[adapter]   at (\xL, 3.8) (proj)  {%
    \fireIcon~~\textbf{MM Projector}\\
    \scriptsize Linear--SwooshR--Linear
  };
  \node[boundbox]  at (\xM, 3.8) (bound) {%
    \fireIcon~~\textbf{Boundary Prompt}\\
    \scriptsize $4 \times 2560$ learnable
  };
  \node[tokenizer] at (\xR, 3.8) (embed) {%
    \textbf{Tokenizer + Embedding}\\
    \scriptsize Qwen3, vocab $151{,}936$
  };

  % vertical arrows from each source to the sequence bottom
  \draw[flow] (proj.north)  -- ([xshift=\xL]seqbox.south)
        node[midway, ratelabel, xshift=0pt] {audio embs};
  \draw[flow] (bound.north) -- ([xshift=\xM]seqbox.south)
        node[midway, ratelabel] {boundary embs};
  \draw[flow] (embed.north) -- ([xshift=\xR]seqbox.south)
        node[midway, ratelabel] {text embs};

  % ====================== Audio column (left) ====================
  \node[encoder] at (\xL, 2.0) (aut) {%
    \frozenIcon~~\textbf{Qwen3-AuT Encoder}\\
    \scriptsize Whisper-style \,$\cdot$\, $\sim 314$\,M
  };
  \node[encoder] at (\xL, 0.4) (front) {%
    \textbf{Log-mel + Conv Sub-sampler}\\
    \scriptsize $128$-bin, $100$\,Hz $\to \sim 13$\,Hz
  };
  \node[audio, minimum height=0.7cm] at (\xL, -1.05) (wave) {%
    \textbf{Audio waveform}\,\,\scriptsize $16$\,kHz
  };
  % small wavy line above the waveform box
  \begin{scope}[line width=0.55pt, draw=cEncB!85]
    \draw[decoration={snake, amplitude=2pt, segment length=3pt}, decorate]
      ([xshift=-1.1cm,yshift=10pt]wave.center) -- ([xshift=1.1cm,yshift=10pt]wave.center);
  \end{scope}
  \draw[flow] (wave)  -- (front);
  \draw[flow] (front) -- (aut);
  \draw[flow] (aut)   -- (proj);

  % ====================== Text column (right) ====================
  \node[promptbox] at (\xR, 2.0) (prompt) {%
    \textbf{Text prompt}\\
    \scriptsize chat template + speech tokens
  };
  \draw[flow] (prompt) -- (embed);

  % ====================== Bottom legend ==========================
  \node[draw=black!35, rounded corners=2.5pt, inner sep=6pt,
        line width=0.4pt, fill=white,
        anchor=north] at ([yshift=-1.4cm]wave.south -| 0,0) (legend) {%
    \sffamily\scriptsize
    \setlength{\tabcolsep}{10pt}%
    \begin{tabular}{@{}lllll@{}}
      \frozenIcon~~frozen (Stage 1) &
      \fireIcon~~trainable &
      \tikz\node[cellText, minimum width=0.32cm, minimum height=0.32cm]{};~text emb &
      \tikz\node[cellBound, minimum width=0.32cm, minimum height=0.32cm]{};~boundary emb &
      \tikz\node[cellAudio, minimum width=0.32cm, minimum height=0.32cm]{};~audio emb \\
    \end{tabular}%
  };

\end{tikzpicture}
\end{document}
