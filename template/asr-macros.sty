% %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% ASR-domain math macros shipped with Amphion PaperKit.
% Loaded explicitly by ASR / Audio-LLM reports; non-ASR reports skip this.
% %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{template/asr-macros}[2026/05/14 v0.1 ASR-domain math macros (Amphion PaperKit)]

\providecommand{\wer}{\mathrm{WER}}
\providecommand{\cer}{\mathrm{CER}}
\providecommand{\mer}{\mathrm{MER}}
\providecommand{\hotwerN}[1]{\wer^{\text{hot}}_{#1}}

% Bold-mark the best (lowest) value in a result table; uses the
% document's native bold face (no font-family override).
\providecommand{\best}[1]{\textbf{#1}}

\endinput
